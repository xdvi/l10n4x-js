/**
 * Mock generated keys — replace with output from `l10n4x build`.
 * Run the real CLI against ../l10n4x examples to produce hashes.
 */
import { defineKeys, type LocaleKey } from "@l10n4x/runtime";

export const Keys = defineKeys({
  CommonWelcome: 0x21048279b702dfbd,
} as const);

export type AppLocaleKey = LocaleKey;